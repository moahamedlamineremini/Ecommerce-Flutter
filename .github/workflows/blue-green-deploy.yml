name: Firebase Blue-Green Deployment (DISABLED)

# Workflow désactivé - utilise flutter_ci.yml à la place
# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main

on:
  workflow_dispatch:  # Permet l'exécution manuelle uniquement

env:
  FLUTTER_VERSION: '3.22.1'
  FIREBASE_PROJECT_ID: 'ecommerce-55dd8-4d606'
# ...existing code...
